# PDF Processing
PyMuPDF>=1.23.0

# API Communication
requests>=2.31.0

# Data Processing & Excel
pandas>=2.1.0
openpyxl>=3.1.0
xlsxwriter>=3.1.0

# Security
bcrypt>=4.1.0

# FastAPI Backend
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-jose[cryptography]>=3.3.0
python-multipart>=0.0.18

# Image Enhancement
Pillow>=10.0.0

# V9 — agentic pipeline
agno>=2.5
pydantic>=2.0
openai>=1.0

# LDAP authentication
ldap3>=2.9
cryptography>=42.0

# Storage providers
boto3>=1.35.0

# Background job queue
rq>=1.16.0
redis>=5.0.0

# PostgreSQL backend (concurrent writes for 10+ users)
psycopg[binary]>=3.2.0
SQLAlchemy[asyncio]>=2.0.0
alembic>=1.13.0

# Rate limiting (Redis-backed, shared across uvicorn + RQ workers)
slowapi>=0.1.9

# Observability (optional — activated only if SENTRY_DSN env var is set)
sentry-sdk[fastapi]>=2.18.0
